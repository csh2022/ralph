# Legacy Ralph Assets

This directory contains the pre-Go implementation and related assets that were moved out of the top-level repository surface.

Contents include:
- shell runner and bootstrap scripts
- prompt files for Codex, Amp, and Claude Code
- legacy skills
- Claude marketplace/plugin files
- flowchart presentation assets
- legacy templates and sample PRD files

These files are preserved for reference and possible migration work, but the active product surface now lives at the repository root as the Go CLI implementation.
